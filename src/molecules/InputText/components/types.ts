export type IconsType = Array<string> | Array<React.ReactNode>

export enum IconPositions {
  LEFT = 'left',
  RIGHT = 'right',
  BOTH = 'both'
}

export enum IconVerticalPositions {
  TOP = 'top',
  BOTTOM = 'bottom'
}

export enum InputLeftPaddings {
  SM = 'sm',
  LG = 'lg'
}
