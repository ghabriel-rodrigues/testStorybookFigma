import css from '@styled-system/css'
import { DsFontSizeLg, DsColorGraySpaceGray, DsSpacingSm } from '@tokens/tokens'

export const LegendBaseCSS = css({
  padding: 0,
  fontSize: DsFontSizeLg,
  color: DsColorGraySpaceGray,
  lineHeight: 1.5,
  fontWeight: 'bold',
  marginBottom: DsSpacingSm
})
