import { DsFontSizeXlg } from '@/tokens/tokens'
import css from '@styled-system/css'

const HintSVGCSS = css({
  fill: 'currentColor',
  display: 'inline-block',
  fontSize: `${DsFontSizeXlg}`,
  width: '24px',
  height: '24px'
})

export { HintSVGCSS }
