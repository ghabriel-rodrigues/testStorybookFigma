import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import Card from './Card'

<Meta title="atoms/Card" component={Card} />

# Card

Este é um componente base. Extremamente simples e criado com finalidade de
definir como um card deve funcionar em termos de cores, padding e formatação básica.

`import { Card } from '@gupy/design-system-v2'`

## Propriedades

<ArgsTable of={Card} />

## Default Card

<Canvas>
  <Story name='Card' args={{}}>
    {(args) => <Card {...args}>Conteúdo do card</Card>}
  </Story>
</Canvas>
