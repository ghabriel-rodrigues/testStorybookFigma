import React from 'react'
import styled from 'styled-components'

export const propTypes = {}

export interface Props {
  text: string
}

const {{pascalCase name}}Base = styled.h1``

const {{pascalCase name}}: React.FC<Props> = ({ text }) => {
  return <{{pascalCase name}}Base>{text}</{{pascalCase name}}Base>
}

export default {{pascalCase name}}
